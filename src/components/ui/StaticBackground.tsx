/**
 * StaticBackground — light theme, no dark overlay.
 * The Figma Hotel Web App design uses a clean white/light gray background.
 */
export default function StaticBackground() {
  return null; // Light theme uses CSS background colors, no fixed image needed
}
